<h1 class="page-header text-center">Welcome to the Movies page!</h1>


<p>
  <form action="/movies" method="post">
    <div class="form-group">
      <label for="movie">Add a Movie</label>
      <input type="text" name="movie" class="form-control" required>
    </div>
    <button name="add_movie">Add Movie</button>
  </form>
</p>


<h4>Here are our current movies:</h4>

<% if @movies.any?() %>
<p>Click on a movie to see information about it: </p>
    <ul>
      <% @movies.each() do |movie| %>
      <li><a href="/movies/<%=movie.id%>"> <%= movie.name() %> </a></li>
      <% end %>
    </ul>

<% else %>

  <h4>Currently there are no movies, add an movie above!</h4>

<% end %>
